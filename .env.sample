# Azure OpenAI
BIG_MODEL=gpt-5.2-codex
SMALL_MODEL=gpt-5.2
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com/
AZURE_OPENAI_API_KEY=<your-key>
AZURE_API_VERSION=2024-10-21

# Cursor -> Azure Responses API 适配所用的部署名（必填，Cursor 相关接口会用它作为 model 字段）
# 例如：你在 Azure OpenAI 中创建的部署名（不是模型名）
CURSOR_AZURE_DEPLOYMENT=<your-deployment>

# Optional: Require Bearer token for /v1/messages* endpoints
ANTHROPIC_AUTH_TOKEN=<your-token>
